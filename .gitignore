# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

.env

# meridian runtime cache (graphs, file hashes, etc.)
.meridian/

# Slither/crytic-compile incidental artifacts. run_slither uses
# cwd=repo so SARIF URIs match the graph; crytic-export lands
# in the repo as a side effect.
crytic-export/
crytic-compile.config.json