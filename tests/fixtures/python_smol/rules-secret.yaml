# Local semgrep rule pack used by tests/test_semgrep.py.
# Half of the split fixture (see rules-exec.yaml).
rules:
  - id: smol-hardcoded-secret
    pattern-regex: '"sk-[a-zA-Z0-9]{32}"'
    message: Hardcoded API key
    languages: [python]
    severity: WARNING
